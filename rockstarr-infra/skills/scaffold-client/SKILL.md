---
name: scaffold-client
description: "This skill should be used when the user asks to \"scaffold a Rockstarr client\", \"set up the rockstarr folder\", \"initialize a new Rockstarr AI client\", or \"create the rockstarr-ai folder\". It creates the canonical /rockstarr-ai/ folder structure inside the current Cowork workspace so every Rockstarr bot has a consistent place to read context and write drafts. Idempotent: re-running is safe and will not overwrite existing files."
---

# scaffold-client

Create the canonical Rockstarr AI folder layout inside the client's Cowork
workspace. Every other Rockstarr skill — and every bot plugin — assumes this
layout exists.

## When to run

- First-time setup for a new client (typical trigger during intake).
- Recovery if someone has accidentally deleted or renamed folders.

## Preconditions

- The user has a Cowork workspace folder selected. If no workspace is active,
  call `request_cowork_directory` to ask them to pick one before proceeding.
- You know the client's name (ask the user if they did not provide it).

## Folder layout to create

Create these directories and placeholder files under the workspace root:

```
/rockstarr-ai/
  /00_intake/
    samples/                              # optional voice samples the client drops in
  /01_knowledge_base/
    /raw/
    /raw/third-party/                     # third-party reference material
    /processed/
    /processed/third-party/               # processed third-party reference
    index.md                              # generated by kb-ingest later
  /02_inputs/
    /content/                             # case-study interview transcripts, content briefs
  /03_drafts/
    /content/
    /social/
    /outreach/
    /replies/
    /nurture/
  /04_approved/
    /content/                             # approved content drafts land here
  /05_published/
    /content/                             # shipped content logs land here
  /06_reports/
    /weekly/
    /monthly/                             # monthly performance reports (content, etc.)
    /data/
  /99_archive/
  client.toml                             # client_id, client_name, created_at
  README.md                               # short explanation of the folders
```

## Steps

1. Confirm the workspace root. Prefer the root of the Cowork-selected folder.
2. Ask the user for `client_id` (kebab-case, e.g. `acme-corp`) and
   `client_name` (display name) if not already supplied. `client_id` must
   match `[a-z0-9-]+`.
3. Create each directory above. Use `mkdir -p` semantics — never overwrite or
   delete existing contents. If `/rockstarr-ai/` already exists, add only the
   missing subdirectories and leave the rest alone.
4. Write `/rockstarr-ai/client.toml` **only if it does not already exist**:

   ```toml
   client_id     = "acme-corp"
   client_name   = "Acme Corp"
   created_at    = "2026-04-20T14:00:00Z"
   rockstarr_infra_version = "0.1.0"
   ```

5. Write `/rockstarr-ai/README.md` only if missing. One paragraph explaining
   that this folder is managed by Rockstarr AI and that drafts live in
   `/03_drafts/`, approvals in `/04_approved/`, and shipped outputs in
   `/05_published/`.
6. Do not create files inside `/01_knowledge_base/processed/`,
   `/01_knowledge_base/raw/`, or their `third-party/` subdirectories.
   Those are managed by `kb-ingest`. Just create the empty directories.

## Output to the user

After scaffolding, print a short summary: the workspace path, the
`client_id`, and a checklist showing which directories were created vs. which
already existed. End with a one-line next-step prompt:

> Next: drop the client workbook (`.docx`) into `/rockstarr-ai/00_intake/`
> and run `ingest-workbook`.

## What NOT to do

- Do not delete or move anything.
- Do not scaffold outside `/rockstarr-ai/` (e.g., do not touch the parent
  Cowork folder).
- Do not write `client-profile.md` or `style-guide.md` — those are outputs of
  other skills.
